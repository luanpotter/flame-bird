name: flame_bird
description: A sample Flappy Bird clone using Flame.

version: 0.2.0

environment:
  sdk: ">=2.10.0 <3.0.0"

dependencies:
  flame:
    path: ../flame
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: false

  assets:
   - assets/images/bird.png
